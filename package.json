{
  "name": "eleventy-plugin-embed-cloudinary",
  "description": "Eleventy plugin to automatically embed images hosted on Cloudinary.",
  "author": "Giacomo Debidda",
  "license": "MIT",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jackdbd/eleventy-plugin-embed-cloudinary.git"
  },
  "main": "dist/.eleventy.js",
  "typings": "dist/.eleventy.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=10"
  },
  "keywords": [
    "11ty",
    "cloudinary",
    "eleventy",
    "eleventy-plugin",
    "image"
  ],
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsdx build --entry ./src/.eleventy.ts --target node && mv dist/index.js dist/.eleventy.js",
    "lint": "tsdx lint",
    "prepare": "tsdx build --entry ./src/.eleventy.ts --target node && mv dist/index.js dist/.eleventy.js",
    "size": "size-limit",
    "size:why": "size-limit --why",
    "start": "tsdx watch --entry ./src/.eleventy.ts --target node --verbose",
    "test:config": "tsdx test --showConfig",
    "test": "tsdx test --detectOpenHandles --coverage=true --verbose"
  },
  "husky": {
    "hooks": {
      "pre-commit": "tsdx lint"
    }
  },
  "prettier": {
    "printWidth": 80,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "module": "dist/eleventy-plugin-embed-cloudinary.esm.js",
  "size-limit": [
    {
      "path": "dist/eleventy-plugin-embed-cloudinary.cjs.production.min.js",
      "limit": "50 B"
    },
    {
      "path": "dist/eleventy-plugin-embed-cloudinary.esm.js",
      "limit": "50 B"
    }
  ],
  "dependencies": {
    "@11ty/eleventy-cache-assets": "^2.2.1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@size-limit/preset-small-lib": "^4.10.2",
    "husky": "^6.0.0",
    "size-limit": "^4.10.2",
    "tsdx": "^0.14.1",
    "tslib": "^2.2.0",
    "typescript": "^4.2.4"
  }
}
